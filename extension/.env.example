# =============================================
# CodeChronicle Extension Configuration
# =============================================

# AWS Configuration (only needed for Cloud AI features)
# Leave empty to run in local-only mode (graph + metrics + blast radius still work)
AWS_API_ENDPOINT=https://usl085fgve.execute-api.us-east-1.amazonaws.com
AWS_REGION=us-east-1

# Set to "true" to enable cloud AI features
ENABLE_CLOUD_AI=true
